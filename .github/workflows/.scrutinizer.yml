name: Tests
on: 
  push:
    branches:
      - master
      
jobs:
  tests:
    name: Unit Testing
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: cd Tasks/UnitTests/UnitTests.csproj
      - run: pwd
      - run: dotnet test
