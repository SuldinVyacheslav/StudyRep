name: Tests
on: 
  push:
    branches:
      - master
      
jobs:
  tests:
    name: Unit Testing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: ls
      - run: cd Tasks | ls
