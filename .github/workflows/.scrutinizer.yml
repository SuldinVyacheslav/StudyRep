name: Tests
on: 
  push:
    branches:
      - master
      
jobs:
  tests:
    name: Unit Testing
    runs-on: ubuntu-latest
    steps:
      - run: |
              cd Tasks
              cd Tasks
              dotnet test
