name: Tests
on: 
  push:
    branches:
      - master
      
jobs:
  tests:
    name: Unit Testing
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: ls
      - run: cd Tasks
      - run: ls
      - run: dotnet test ~/Tasks/UnitTests/UnitTests.csproj
