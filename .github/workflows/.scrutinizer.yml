name: Tests
on: 
  push:
    branches:
      - master
      
jobs:
  tests:
    name: Unit Testing
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - run: pwd
      - run: cd Tasks\UnitTests
      - run: pwd
      - run: dotnet test ~/UnitTests.csproj
